# 데이터 수집·분석용 파이썬 패키지
#
# ⚠️ 버전을 고정한다(2026-08-04). 이 파이프라인은 매일 무인으로 도는데,
#    yfinance·FinanceDataReader는 파괴적 변경이 잦아 핀이 없으면 어느 날 갑자기 수집이 죽는다.
#    올릴 때는 로컬에서 `python scripts/collect_data.py kr` 를 한 번 돌려 확인하고 올린다.
#    (하한만 두는 이유: 마이너 보안 패치는 받되 메이저 변경은 막는다)

yfinance>=1.5.1,<2            # 지수·주가·환율·VIX 등 시세 데이터
pandas>=2.3.3,<3              # 데이터 처리·지표 계산
requests>=2.34.2,<3           # HTTP 요청 (특정 사이트 크롤링 확장용)
beautifulsoup4>=4.15.0,<5     # HTML 파싱 (크롤링 확장용)
lxml>=6.1.1,<7                # 빠른 HTML/XML 파서
pyyaml>=6.0.3,<7              # config/watchlist.yaml 읽기
finance-datareader>=0.9.202,<1  # 한국 종목 유니버스/시세 (비인기종목 스크리너)
matplotlib>=3.11.0,<4         # 차트 생성
